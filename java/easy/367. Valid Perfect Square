public class Solution {
    public boolean isPerfectSquare(int num) {
        if(Math.floor(Math.pow(num,0.5))==Math.pow(num,0.5)){
           return true; 
        }
        else{
            return false;
        }
    }
}
